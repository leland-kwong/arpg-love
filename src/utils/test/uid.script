local uid = require("utils.uid")

local function runTests()
	local id1 = uid()
	local id2 = uid()
	assert(id1 ~= id2, "uids should not be equal")
end

function init(self)
	runTests()
end

function on_reload(self)
	runTests()
end
